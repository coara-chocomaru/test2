name: Build ARM64 Binary for Android 5.1+

on:
  push:
    branches: check3
  pull_request:
    branches: check3
  workflow_dispatch:

jobs:
  build-android-arm64:
    runs-on: ubuntu-latest
    env:
      NDK_VERSION: r10e
      NDK_HOME: ${{ github.workspace }}/android-ndk-r10e
      TARGET_ABI: arm64-v8a
      API_LEVEL: android-22
      MODULE_NAME: check_poc
      SOURCE_FILE: check_poc.c

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Cache Android NDK
        id: cache-ndk
        uses: actions/cache@v4
        with:
          path: ${{ env.NDK_HOME }}
          key: ndk-${{ env.NDK_VERSION }}-${{ runner.os }}

      # r10e は .bin（自己解凍形式）
      - name: Download and setup Android NDK r10e
        if: steps.cache-ndk.outputs.cache-hit != 'true'
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r10e-linux-x86_64.bin
          chmod +x android-ndk-r10e-linux-x86_64.bin
          ./android-ndk-r10e-linux-x86_64.bin
          echo "${{ env.NDK_HOME }}" >> $GITHUB_PATH
          echo "NDK_HOME=${{ env.NDK_HOME }}" >> $GITHUB_ENV

      - name: Ensure source file exists
        run: |
          if [ ! -f "${{ env.SOURCE_FILE }}" ]; then
            C_FILE=$(ls *.c 2>/dev/null | head -n1)
            [ -n "$C_FILE" ] && mv "$C_FILE" "${{ env.SOURCE_FILE }}"
          fi
          ls -la *.c *.h 2>/dev/null || true

      - name: Generate Android.mk
        run: |
          cat > Android.mk << 'EOF'
          LOCAL_PATH := $(call my-dir)
          include $(CLEAR_VARS)

          LOCAL_MODULE := ${{ env.MODULE_NAME }}
          LOCAL_SRC_FILES := ${{ env.SOURCE_FILE }}

          LOCAL_CFLAGS := -fPIE -Wall -Wextra -O2 -pthread
          LOCAL_LDFLAGS := -fPIE -pie

          include $(BUILD_EXECUTABLE)
          EOF
          cat Android.mk

      - name: Build with ndk-build
        run: |
          export PATH="${{ env.NDK_HOME }}:${{ env.NDK_HOME }}/bin:$PATH"
          ndk-build \
            NDK_PROJECT_PATH=. \
            APP_BUILD_SCRIPT=./Android.mk \
            APP_ABI=${{ env.TARGET_ABI }} \
            APP_PLATFORM=${{ env.API_LEVEL }} \
            -j$(nproc) V=1

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.MODULE_NAME }}_arm64_android5.1
          path: libs/${{ env.TARGET_ABI }}/${{ env.MODULE_NAME }}
          if-no-files-found: error
